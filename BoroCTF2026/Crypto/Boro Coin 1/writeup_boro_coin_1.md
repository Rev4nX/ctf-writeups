# boroCTF 2026 — Boro Coin 1 (Crypto, 100pts)

**Author:** Franklin  
**Category:** Crypto  

## Description

> We have a government seized Boro Wallet with 51.42 bc before any transactions occurred. Sadly, the suspect refuses to give us the private key. Can you retrieve the key?
>
> Boro Coin uses standard secp256k1 ECDSA. A transaction hash is generated by taking the SHA-256 of the string format: "sender:recipient:amount" (Colons included!)

## Files

- `transactions.json`

---

## Solution

### Step 1 — Understand the cryptographic setup

Boro Coin uses **secp256k1 ECDSA** — the same elliptic curve algorithm as Bitcoin. Each transaction is signed with:
- A private key `d` (what we want to recover)
- A per-signature random number `k` (the nonce)

Each signature is a pair `(r, s)` where:
- `r = (k·G).x mod n` — depends **only on k**, not on the message
- `s = k⁻¹ · (hash + r·d) mod n` — depends on k, hash, and private key d

### Step 2 — Identify the vulnerability

Scan `transactions.json` for signatures with identical `r` values. Transactions 4 and 19 both have:

```
r = 2cbda85fc21f5e62f94d8378d2dad1a05bc5d5522d5a717f2bdf1df13d558ec7
```

Identical `r` means identical `k` was used — this is the **nonce reuse vulnerability**.

### Step 3 — Recover the nonce k

With two signatures `(r, s1)` and `(r, s2)` over hashes `h1` and `h2`:

```
s1 = k⁻¹ · (h1 + r·d) mod n
s2 = k⁻¹ · (h2 + r·d) mod n
```

Subtracting eliminates `d`:

```
s1 - s2 = k⁻¹ · (h1 - h2) mod n
k = (h1 - h2) · (s1 - s2)⁻¹ mod n
```

### Step 4 — Recover the private key d

Once `k` is known, solve for `d` from the signature equation:

```
d = (s·k - h) · r⁻¹ mod n
```

### Step 5 — Solver

```python
from hashlib import sha256

# secp256k1 order
n = 0xFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFEBAAEDCE6AF48A03BBFD25E8CD0364141

def parse_der(sig_hex):
    sig = bytes.fromhex(sig_hex)
    assert sig[0] == 0x30
    idx = 2
    assert sig[idx] == 0x02
    rlen = sig[idx+1]
    r = int.from_bytes(sig[idx+2:idx+2+rlen], 'big')
    idx = idx+2+rlen
    assert sig[idx] == 0x02
    slen = sig[idx+1]
    s = int.from_bytes(sig[idx+2:idx+2+slen], 'big')
    return r, s

def tx_hash(sender, recipient, amount):
    msg = f'{sender}:{recipient}:{amount}'
    return int.from_bytes(sha256(msg.encode()).digest(), 'big')

# Transaction 4: Suspect -> FranklinGothic, 21.68
r4, s4 = parse_der('304402202cbda85fc21f5e62f94d8378d2dad1a05bc5d5522d5a717f2bdf1df13d558ec702204b2f38c18c2a933f81112350ae048f0162feaaed599f827180944ea3203570de')
z4 = tx_hash('Suspect', 'FranklinGothic', '21.68')

# Transaction 19: Suspect -> ForeverFlames, 45.46
r19, s19 = parse_der('304402202cbda85fc21f5e62f94d8378d2dad1a05bc5d5522d5a717f2bdf1df13d558ec702207db2d815212aab6b986d0a403b724ad5fd57d2d9e826bf2893e29d9d179d59f3')
z19 = tx_hash('Suspect', 'ForeverFlames', '45.46')

# Recover k
k = ((z4 - z19) * pow(s4 - s19, -1, n)) % n

# Recover private key d
d = ((s4 * k - z4) * pow(r4, -1, n)) % n
print('private key:', hex(d)[2:])
```

Output: `1b7ba9dafeb7c7a30fd8043a656c3ab89509db070dbd48b593d8e266b56ca22d`

---

## Flag

```
boroCTF{1b7ba9dafeb7c7a30fd8043a656c3ab89509db070dbd48b593d8e266b56ca22d}
```

---

## Key takeaways

- **ECDSA nonce reuse is catastrophic** — if two signatures share the same `k`, the private key is fully recoverable with high school algebra (modular arithmetic). No brute force required.
- The giveaway is two signatures with identical `r` value — `r` depends only on `k`, so equal `r` = equal `k`
- Transaction hashes are reconstructed from the plaintext string `"sender:recipient:amount"` via SHA-256 — always check how hashes are derived, it's specified in the challenge
- DER encoding is the standard binary format for ECDSA signatures — parse `30 02 r 02 s` to extract `r` and `s`
- `pow(x, -1, n)` in Python 3.8+ computes the modular inverse directly — no need for the extended Euclidean algorithm manually
- This exact attack took down the Sony PlayStation 3 signing key in 2010 and drained Bitcoin wallets in 2013 — real-world consequences of bad RNG
